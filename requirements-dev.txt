# Test tooling only. The packaged workspace does not need Python.
# Qualified with Python 3.12; legacy formatting tests require Python >= 3.10.
greenlet==3.5.5
lxml==6.1.3
playwright==1.58.0
pyee==13.0.1
python-docx==1.2.0
typing_extensions==4.16.0
